
<%if current_user.street? %>

<%= form_for(meal) do |f| %>
  <% if meal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meal.errors.count, "error") %> prohibited this meal from being saved:</h2>

      <ul>
      <% meal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Nom %>
    <%= f.text_field :name, :class => 'form-control'  %>
  </div>

  <div class="field">
    <%= f.label :Entrée %>
    <%= f.text_field :entree, :class => 'form-control'  %>
  </div>

    <div class="field">
      <%= f.label :description %>
      <%= f.text_field :edesc, :class => 'form-control'  %>
    </div>

  <div class="field">
    <%= f.label :Plat %>
    <%= f.text_field :plat, :class => 'form-control'  %>
  </div>

    <div class="field">
      <%= f.label :description %>
      <%= f.text_field :pdesc, :class => 'form-control'  %>
    </div>

  <div class="field">
    <%= f.label :Dessert %>
    <%= f.text_field :dessert, :class => 'form-control'  %>
  </div>

    <div class="field">
      <%= f.label :description %>
      <%= f.text_field :ddesc, :class => 'form-control'  %>
    </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>

  <div class="field">
    <%= f.label :participants %>
    <%= f.select(:nbpart, 1...5) %>
  </div>

  <%= f.hidden_field :user_fname, :value => current_user.fname %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<% else %>
<p> Vous devez rentrer votre adresse avant de créer un repas </p>
<button type="button" class="btn btn-default navbar-btn btn-paupiette"><%= link_to 'Editer mon profil', edit_user_registration_path %></button>
<%end%>
